import 'package:flutter/material.dart';

const Icon kDogruIcon = Icon(Icons.mood, color: Colors.green);
///const (sabit) - (k) harfi kullanılır.

const Icon kYanlisIcon = Icon(Icons.mood_bad, color: Colors.red);
///const (sabit) - (k) harfi kullanılır.

